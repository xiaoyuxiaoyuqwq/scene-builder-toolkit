# 场景构建 · 多维度扩展工作流 V4

> V3 基础 + 洛都市 6 轮迭代 + 程序化海岸/碰撞检测网格/InstancedMesh 全面化 / LOADING 模式纠正

---

## 流程概要

```
用户一句话描述
    ↓
[第0维] 识别场景类型 → 加载对应子维度
    ↓
[轮次1] 核心氛围（类型决定子维度）
    ↓
[轮次2] 视角与运镜
    ↓
[轮次3] 细节精度 + 色彩主题
    ↓
[轮次4] 特殊元素
    ↓
确认 → 生成完整 Three.js HTML（单文件）
```

---

## 第 0 维：场景类型（决定后续所有子维度）

| 类型 | 子维度体系 | 建模重点 | 示例 |
|------|-----------|---------|------|
| **古风城镇** | 时辰/季节/天气/区域/建筑精度 | 屋顶/立柱/飞檐/台基分层 | 长安坊市 |
| **太空科幻** | 光照条件/天体背景/结构形式/机械精度 | 环形桁架/对接口/太阳能板/隔热盾 | 空间站对接 |
| **自然地形** | 生物群系/季节/天气/地形类型 | 程序化地形/植物分布/水面 | 山脉/森林/海岛 |
| **赛博霓虹** | 时辰/天气/建筑密度/霓虹强度 | 反射面/发光带/全息投影 | 雨夜街道 |
| **城市街区** | 时辰/季节/建筑风格/城区密度 | 摩天楼（退台+幕墙+尖顶多件叠加）/Palms multi-part/高速曲线 | 洛都市（山腰俯瞰） |
| **游戏/互动** | 角色/操控/物理/目标 | 第三人称跟随+碰撞破坏+碎片物理+计分 | 怪兽大破坏 |
| **定制** | 用户自定子维度 | 按描述决定 | 任意 |

---

## 子维度详细定义

### A. 古风城镇

#### 时辰
| 选项 | 色温 | 阴影 | 氛围关键词 |
|------|------|------|-----------|
| 黎明 | 冷紫→暖橙渐变 | 长、软 | 静谧、苏醒、朝霞 |
| 日昃 | 白黄 | 短、硬 | 明亮、热闹、清晰 |
| 黄昏（推荐） | 橙红→紫蓝 | 长、暖 | 温暖、炊烟、归家 |
| 入夜 | 深蓝→黑 | 月光冷 | 灯笼、静谧、星空 |

#### 季节
| 选项 | 树冠色 | 地面色 | 天空偏移 |
|------|--------|--------|----------|
| 春 | 嫩绿 #5a9f4a | 湿润褐 #8a7a60 | 粉白晕 |
| 夏 | 浓绿 #3a7d3a | 干暖褐 #9c8a70 | 晴蓝 |
| 秋（推荐） | 金黄 #8a7d2a / 红褐 | 落叶褐 #a08a60 | 暖橙 |
| 冬 | 枯枝 #5a4a3a / 雪冠白 | 灰褐 #7a7a7a | 灰白 |

#### 天气
| 选项 | Fog | 光照强度 | 特效 |
|------|-----|----------|------|
| 晴 | 0.003 | 100% | 无 |
| 薄云 | 0.005 | 80% | 柔和 |
| 雾 | 0.012 | 60% | 景深感 |
| 雨 | 0.008 | 50% | 雨丝粒子 + 湿润反光 |
| 雪 | 0.008 | 70% | 雪粒子 + 屋顶白冠 |

#### 区域焦点
| 选项 | 布局特征 | 建筑类型侧重 |
|------|----------|-------------|
| 坊内民居 | 规整院落，树多 | small + large 为主 |
| 东西街市 | 沿街商铺密集 | shop + large |
| 寺庙 | 中心高塔 + 开阔 | pagoda + mansion |
| 城门 | 城墙 + 门楼 + 瓮城 | 防御结构为主 |
| 全景坊市 | 5×5 坊 + 街 + 墙 | 所有类型混合 |

---

### B. 太空科幻

#### 光照条件
| 选项 | 色温 | 阴影 | 视觉 |
|------|------|------|------|
| 向阳面 | 白黄 | 硬、锐利 | 高对比、表面过曝 |
| 晨昏交界（推荐） | 暖橙+深蓝渐变 | 过渡柔 | 戏剧感最强 |
| 背阳面 | 冷蓝+黑 | 暗、软 | 仅靠环境反射光 |
| 星云漫射 | 紫粉漫射 | 极软 | 彩色星云照亮 |

#### 天体背景
| 选项 | 实现方式 | 视觉 |
|------|---------|------|
| 地球轨道 | ShaderMaterial 球体 + 昼夜 + 大气辉光 | 蓝白地球占背景 |
| 深空星场 | Points 粒子 | 无限星空 |
| 气态行星 | 多层彩纹球体 + 环 | 巨行星+光环 |
| 月球表面 | 灰色陨石坑球体 | 荒凉地表 |

#### 结构形式
| 选项 | 几何模式 | 动画 |
|------|---------|------|
| 环形旋转 | Torus + 模块 + 辐条 | 环自转 |
| 轴向串连 | 沿轴 Cylinder 串列 | 无/微振 |
| 树状分枝 | 核心+辐射臂 | 分支可动 |
| 不规则集群 | 多个散乱模块 | 各模块独立漂移 |

---

### C. 自然地形

#### 生物群系
| 选项 | 地形色 | 植被 | 天空 |
|------|--------|------|------|
| 温带草原 | 绿褐 | 草+灌木+树 | 蓝 |
| 沙漠 | 沙黄 | 仙人掌+枯石 | 白尘 |
| 雪山/苔原 | 灰白 | 松林+雪 | 青灰 |
| 热带雨林 | 深绿 | 密林+藤蔓 | 多云 |
| 海岛/海岸 | 沙+蓝水 | 棕榈+礁石 | 晴蓝 |

---

## 视角与运镜（全类型共享）

| 选项 | Camera | 控制 | 适用场景 |
|------|--------|------|----------|
| 俯瞰沙盘 | Perspective 35° | Orbit + autoRotate | 全景展示 |
| 平视/旁观 | Perspective 50° | Orbit 限制俯仰 | 身临其境 |
| 漫游探索 | Perspective 60° | PointerLock + WASD | 第一人称 |
| 电影运镜 | Perspective 40° 动态 | GSAP 路径/lerp | 叙事 |
| 对接接近 | Perspective 32° 固定偏移动画 | Orbit 初始不转 | 太空对接 |

---

## 细节精度（全类型共享）

| 等级 | 特征 | 适用 |
|------|------|------|
| **极致** | 每个子部件 10+ 几何体叠加，栅格线、铆钉、舷窗、焊线 | 高端展示 |
| **写实**（推荐） | 每个子部件 5-10 几何体，立柱/飞檐/桁架/对接口/翅片 | 主力场景 |
| **标准** | 2-3 几何体/部件 | 快速原型 |
| **极简** | 单几何体/部件 | 风格化/低性能 |

---

## 色彩主题（全类型共享）

| 主题 | 基色 | 强调色 | 适合 |
|------|------|--------|------|
| 暖金 | 米白+土黄 | 朱红+金 | 古风黄昏/秋 |
| 冷科技 | 灰白+金属银 | 蓝+暗橙 | 太空写实 |
| 赛博紫 | 深紫+黑 | 品红+青 | 赛博/幻境 |
| 素雅（推荐） | 灰白+浅褐 | 靛蓝+竹青 | 冬/雨/黎明 |
| 华贵 | 朱红+金 | 绿琉璃+青 | 寺庙/盛大 |

---

## 特殊元素（按类型）

### 古风城镇
- 灯笼（沿街+门前，夜晚发光）
- 旗帜/酒幡（商铺标识）
- 马车（街道）
- 行人 NPC（动态/静态）
- 流水/池塘（坊内景观）
- 石桥（跨街水渠）
- 古树（大型特殊树木）
- 石碑/香炉（寺庙前）

### 太空科幻
- 对接飞船（动态接近）
- 舱外宇航员（EVA 小人物）
- 机械臂（加拿大臂）
- 旋转闪烁灯
- 推进器尾焰粒子
- 漂浮太空碎片
- 全息投影环
- 散热板（动态热红）

### 自然地形
- 飞鸟（V 形粒子）
- 蝴蝶/萤火虫
- 流动河水
- 雾气贴地层
- 篝火/烟雾
- 野生动物
- 石阵/遗迹
- 彩虹/雷暴

---

## 建模方法论（核心）

### 原则：部件拆解叠加法

每个"东西"不是 1 个几何体，而是 5-15 个几何体的叠加组合。

**长安坊市建筑 =**
```
平台1（大石）+ 平台2（小石）+ 墙体（Box）+ 
立柱×4（Cylinder）+ 前柱×N（Cylinder）+ 
飞檐（Box 挑出）+ 曲面屋顶（Cylinder 4段 + 顶点变形）+ 
脊饰×2（Cylinder 倾斜）
```

**空间站环形段 =**
```
基环（Torus）+ 内环（Torus）+ 外环（Torus）+ 
居住舱（Cylinder 横放）+ 内衬（Cylinder）+ 
端肋×2（Torus）+ 散热板（Box）+ 散热翅片×4（Box）+ 
舷窗×4（Circle 发光）
```

**空间站核心舱 =**
```
主筒体（Cylinder）+ 上锥（Cylinder）、下锥（Cylinder）+ 
环箍×3（Torus）+ 对接口×3（法兰+环+导瓣×3+导杆×2+LED×2）+ 
穹顶观察窗（Sphere 切半 + 6 Circle 窗）+ 天线（Cylinder 杆 + Sphere 碗 + 馈源）
```

**空间站太阳能板（每组） =**
```
桁架臂（Cylinder）+ 万向节（Cylinder）+ 
主梁（Cylinder）+ 7段电池片 ×（面板 Plane + 边框 Box + 4栅格线 Box）
```

### 代码组织模式

```javascript
// 1. 全局色板对象（单一来源，方便主题切换）
const M = {
  hull:     new MeshStandardMaterial({ color: 0xd4d4d4, roughness: 0.25, metalness: 0.5 }),
  hullDark: new MeshStandardMaterial({ color: 0x606060, roughness: 0.4,  metalness: 0.3 }),
  truss:    new MeshStandardMaterial({ color: 0x909090, roughness: 0.35, metalness: 0.4 }),
  // ...
};

// 2. 每个 create* 函数返回 Group，内部自由创建子 Mesh（复用 M.*）
function createFoo(param) {
  const g = new THREE.Group();
  // 子部件1
  const part1 = new THREE.Mesh(geo1, M.hull);
  part1.castShadow = true;
  g.add(part1);
  // 子部件2
  // ...
  return g;
}

// 3. build* 函数组装全部 create* 结果
function buildStation() {
  const station = new THREE.Group();
  for (let i = 0; i < N; i++) {
    station.add(createRingSegment(i));
  }
  station.add(createHub());
  return station;
}
```

### 关键技巧速查

| 技巧 | 用途 | 示例 |
|------|------|------|
| CylinderGeometry(0.05, R, H, 4) | 四面锥顶（中式屋顶） | 长安屋顶 |
| 顶点位移 curve(1-t) | 屋顶翘角 | `1+0.18*pow(1-t,2.5)` |
| TorusGeometry 叠同心层 | 环形加强结构 | 空间站环 |
| PlaneGeometry + Box 栅格线 | 太阳能板/电池片 | 空间站面板 |
| SphereGeometry(半个) | 穹顶/观察窗 | 穹顶窗 |
| CircleGeometry 发光 | 舷窗/指示灯 | 居住舱/对接口 |
| ShaderMaterial + sunDir uniform | 地球昼夜+城市夜光 | 晨昏交界 |
| DoubleSide + depthWrite:false | 大气辉光壳 / 玻璃幕叠加 | 大气层 / 城市夜景 |
| lerpVectors + easeInOut | 缓动对接动画 | 飞船接近 |
| logarithmicDepthBuffer: true | 大跨度场景防远处闪烁 | 城市全景 / 太空 |
| ground.y = -0.02 | 防建筑底面与地面 z-fighting | 所有场景 |
| renderOrder + depthWrite:false | 发光叠加层不遮挡主体 | 窗光/辉光面 |
| alphaTest = 0.5（代替 opacity） | 二值透明，比半透明性能好 3x | 树叶/栅格 |
| InstancedMesh | 相同几何体重复 > 100 个时用 | 城市窗户/路灯/树木 |
| SMAAPass | 后处理抗锯齿，代替硬件 MSAA | EffectComposer 场景 |
| Custom BufferGeometry + 正弦扰动 | 程序化海岸线/自然地形边缘 | 沙滩水岸 |
| CanvasTexture 程序化纹理 | 生成颗粒噪点/沙纹/木纹，免网络纹理加载 | 沙滩/地面/面板 |
| `overlaps()` 碰撞检测 | 建筑/物体网格布局防叠放 | 城市街区 |
| 多层叠加（干沙+湿沙+水） | 自然过渡：干沙→湿沙→水，避免硬边界 | 海岸线 |
| LOADING 同步隐藏 + 超时兜底 | 不依赖 CDN 异步回调，场景构建完就隐藏 + `setTimeout(…, 5000)` 保底 | 所有场景 |
| OrbitControls.target.lerp | 第三人称跟随：相机目标缓动到角色位置 | 怪兽大破坏 |
| 建筑碰撞自动摧毁 | 怪物行走碰撞建筑 AABB 时触发 smash()，手感即破坏 | 怪兽大破坏 |
| 碎片物理系统 | debris 数组 + `vel.y -= g*dt` + 地面反弹衰减 + 生命周期 | 破坏效果 |
| 怪物步行循环 | `Math.sin(walkPhase)` → 躯干起伏 + 手臂/腿交替摆动 | 角色动画 |

---

## GitHub 参考案例锚点

### 必学项目
| 项目 | Stars | 核心价值 |
|------|-------|---------|
| [**brunosimon/folio-2019**](https://github.com/brunosimon/folio-2019) | 4.7k | 程序化场景架构标杆 |
| [**dragonir/3d**](https://github.com/dragonir/3d) | 2.9k | 纯 Three.js 多场景合集 |
| [**fireship-io/threejs-scroll-animation-demo**](https://github.com/fireship-io/threejs-scroll-animation-demo) | 1.7k | 滚动叙事 |
| [**craftzdog/craftzdog-homepage**](https://github.com/craftzdog/craftzdog-homepage) | 2.5k | 模块化场景架构 |

### 专项参考
| 类别 | 项目 | Stars | 用途 |
|------|------|-------|------|
| 程序化地形 | [**THREE.Terrain**](https://github.com/IceCreamYou/THREE.Terrain) | 882 | 地形+生态生成 |
| 程序化星球 | [**ProceduralTerrains**](https://github.com/ZyFou/ProceduralTerrains) | 379 | 星球/无限地形 |
| 程序化城市 | [**city-tour**](https://github.com/jstrait/city-tour) | 86 | 区块城市生成 |
| 程序化建筑 | [**threejs-procedural-building-generator**](https://github.com/Aljullu/threejs-procedural-building-generator) | 45 | 参数化建筑 |
| 程序化树木 | [**SeedThree**](https://github.com/SkyeShark/SeedThree) | 67 | L-system 植物 |
| 银河/星场 | [**galaxy-explorer**](https://github.com/zjoooooo/galaxy-explorer) | 101 | 银河臂粒子 |
| WFC 结构 | [**three-wfc**](https://github.com/Fennec-hub/three-wfc) | 77 | 约束式布局生成 |
| 水面/特效 | [**hepengwei/visualization-collection**](https://github.com/hepengwei/visualization-collection) | 4.6k | 百种视觉效果 |
| 乐高自然 | [**lego-stylized-nature**](https://github.com/hexianWeb/lego-stylized-nature) | 49 | 体素自然世界 |

---

## AI 执行指令

### 第一步：类型识别
用户输入 → 匹配场景类型 → 若模糊则反问问清

### 第二步：逐轮确认（每轮 ≤ 3 问）
```
轮次 1: 核心氛围（类型相关的子维度，如时辰/光照/背景）
轮次 2: 视角 + 运镜方式
轮次 3: 细节精度 + 色彩主题
轮次 4: 特殊元素（多选）
```

每轮结束后总结已有选择。

### 第三步：定稿确认
展示完整配置摘要 → 用户确认或微调。

### 第四步：代码生成

**强制规范：**
1. 单文件 HTML（CDN importmap 加载 Three.js，统一用 `cdn.jsdelivr.net`，弃用 unpkg）
2. 全局 Material 对象 `M.*` 集中管理，**不在 create* 函数内 new Material**
3. 每个子结构用独立 `create[Name]` 函数
4. 函数返回 `THREE.Group`，内部叠加 5-15 个几何体
5. 地面 y = -0.02（防底部 z-fighting）
6. 所有 create* 内 Mesh 默认 `castShadow = true` / `receiveShadow = true`，发光叠加层除外（`castShadow = false`）
7. 发光/半透明叠加层必须设 `depthWrite = false` + `renderOrder = 1`
8. 后处理：RenderPass → UnrealBloomPass → SMAAPass（按此顺序）
9. 场景跨度 > 50 单位开启 `logarithmicDepthBuffer: true`
10. OrbitControls 默认（特殊视角自动调整）
11. 动画循环：更新粒子/缓动，用 `composer.render()` 代替 `renderer.render()`
12. CDN importmap 所有 import 用 importmap 映射，不用裸 URL import

**参数映射模板：**

```javascript
// ═══ 模板参数 ═══
// → 由维度选择填入

// 场景类型 → scene setup, material palette, create* 函数集
// 时辰/光照 → sunPos, sunColor, intensity, ambient, bloom, fog, bgColor, exposure
// 季节/生物群系 → 树色/地面色/植被色
// 天气/环境 → fog.exp, 粒子系统, 特殊几何体
// 视角 → camera.position, controls 设置
// 精度等级 → 每个 create* 内部的子部件数量
// 色彩主题 → 全局 M.* 色板完全替换
// 特殊元素 → 额外 create* 调用 + 动画
```

---

## 设计原则

1. **类型先行** — 场景类型决定所有后续子维度，不能混用
2. **部件加法** — 永远不要用 1 个几何体代表 1 个物体，拆 5-15 件叠加
3. **材质共享** — M.* 对象全局唯一，不在 create* 内 new Material（性能+一致性）
4. **先氛围后细节** — 时辰/光照 > 视角 > 区域 > 装饰
5. **少即是多** — 每轮 ≤ 3 问
6. **推荐优先** — 视觉最优选项标注"（推荐）"
7. **自定义出口** — 每维最后一项总是"自定义"
8. **地面永负** — 所有地面的 y 坐标 ≤ -0.02，防止与建筑底面 z-fighting
9. **共面必错开** — 两个面在同一位置至少错开 0.001，否则出现条纹闪烁
10. **大场景开对数深** — 场景跨度 > 50 单位必须 `logarithmicDepthBuffer: true`
11. **后处理关 MSAA** — 用 EffectComposer 时 `antialias: false`，加 SMAA Pass 代替
12. **CDN 统一 jsdelivr** — 弃用 unpkg，使用 `cdn.jsdelivr.net` 支持 importmap
13. **网格布局 + 碰撞检测** — 城市建筑不随机撒点：预定义网格坐标 + `overlaps()` 函数校验间距 ≥ 0.08，杜绝叠楼
14. **建筑类型系统** — 不要清一色 Box：`box`/`podium`(宽底座 1.4x + 退台冠 0.65x)/`tower`(底座+塔身+退台冠) 三种造型混合
15. **海岸线程序化** — 沙滩禁用直线矩形：`BufferGeometry` 正弦扰动水岸线 + Canvas 颗粒纹理 + 多层(干沙/湿沙带)增强真实感

---

## 性能守则

### 规模与策略

| 规模 | 建议 |
|------|------|
| < 200 个 Mesh | 直接创建，无需优化 |
| 200-500 个 Mesh | 共享 Material，减少 castShadow |
| 500-2000 个 Mesh | 考虑 InstancedMesh / 几何体合并 |
| > 2000 个 Mesh | 必须用 InstancedMesh + 简化几何体 |

### 渲染器

| 设置 | 建议 |
|------|------|
| antialias | 不用 EffectComposer 时开启 MSAA；用了则关闭并用 SMAA/FXAA |
| shadowMap.type | PCFSoftShadowMap（柔和）或 PCFShadowMap（锐利） |
| shadowMap.mapSize | 主力场景 2048，远距离 1024 |
| toneMapping | ACESFilmicToneMapping（所有场景），exposure 0.8-1.2 |
| pixelRatio | 桌面 2，移动端强制 ≤ 2 |
| logarithmicDepthBuffer | 场景跨度 > 50 单位时开启（如城市全景、太空） |
| powerPreference | `"high-performance"`，确保使用独立显卡 |
| preserveDrawingBuffer | 默认 false，除非需要截图/录屏 |

### CPU 优化

| 技巧 | 说明 |
|------|------|
| 不在渲染循环内 new 对象 | 对象创建昂贵，复用 Vector 用 `.set()` |
| `object.matrixAutoUpdate = false` | 静态/很少移动的对象手动调用 `updateMatrix()` |
| `light.visible = false` 代替增删光源 | 光源增删触发 Shader 重编译 |
| `scene.overrideMaterial` 诊断瓶颈 | 替换所有材质为 MeshBasicMaterial，帧率提升=GPU卡瓶颈，不提升=CPU卡瓶颈 |
| 静态场景仅相机移动时渲染 | OrbitControls 监听 `change` 事件按需 render，省电省风扇 |

### GPU 优化

| 技巧 | 说明 |
|------|------|
| 少用直射光 | DirectionalLight/SpotLight/PointLight 昂贵，尽量 ≤ 3 个 |
| 透明物体尽量少 | `opacity < 1` 很费 GPU，用 `alphaTest = 0.5` 代替（二值透明，更快） |
| 阴影仅更新必要帧 | 静态场景给 `shadow.autoUpdate = false`，变化时手动 `needsUpdate = true` |
| 阴影贴图越小越好 | 距离远时 512 足够，shadow camera frustum 尽量紧 |
| InstancedMesh | 相同几何体 > 100 个用 InstancedMesh 代替逐个 Mesh |
| MeshLambertMaterial | 哑光面用 Lambert 代替 Standard（更快） |

### 纹理

| 规则 | 原因 |
|------|------|
| 所有纹理尺寸 2 的幂 | NPOT 纹理不支持 mipmap 和 repeat |
| 最小尺寸够用即可 | 256×256 的平铺纹理可能效果就足够 |
| JPG vs PNG 文件大小不同、GPU 内存相同 | 同尺寸纹理会占用同量显存 |

### 分析工具

| 工具 | 用途 |
|------|------|
| `renderer.info.render.calls` | 打印 draw calls 数量 |
| `renderer.info.render.triangles` | 三角面总数 |
| [stats.js](https://github.com/mrdoob/stats.js) | 实时 FPS 面板 |
| [Spector.js](https://spector.babylonjs.com/) | Chrome 扩展，逐帧分析 draw commands |
| `--disable-gpu-vsync` | Chrome 启动参数解除帧率限制，测量真实性能 |

### Bloom 强度参考

| 类型 | 强度 | 半径 | 阈值 |
|------|------|------|------|
| 古风 | 0.15-0.20 | 0.3 | 0.1 |
| 太空 | 0.10-0.15 | 0.3 | 0.1 |
| 赛博/幻境 | 0.30-0.50 | 0.3 | 0.05 |
| 城市黄昏 | 0.10-0.15 | 0.3 | 0.1 |

---

## 防穿模与 z-fighting

### 常见问题与修复

| 问题 | 表现 | 原因 | 修复 |
|------|------|------|------|
| 地面与建筑底部面闪烁 | 建筑下方出现条纹跳动 | 地面 y=0，建筑底面 y=0，重叠 | 地面 y = -0.02，建筑底 y = 0 |
| 远距离建筑闪烁 | 远处楼群抖动 | 深度缓冲精度耗尽 | 开启 `logarithmicDepthBuffer: true` |
| 两个面在同一位置 | 亮暗交替闪烁 | z-fighting，GPU 算不出谁在前 | 错开至少 0.001 单位 |
| 场景死黑无显示 | 全黑画面 | camera far 太大或 near 太小 | 收紧 frustum，far 不超过场景深度 2 倍 |
| 雾色与天空不匹配 | 远处物体颜色跳变 | fog color ≠ scene.background | 两者用同一颜色 |
| 阴影斑块/条纹 | 表面有脏条纹 | shadow bias 太小 | bias = -0.002 ~ -0.005，随场景增大调大 |
| 两个重叠透明面 | 背后的面消失 | 透明排序冲突 | 用 `depthWrite: false` 标记叠加层 |

### 防穿模编码规范

```javascript
// ✅ 正确：地面比建筑略低
const ground = new THREE.Mesh(plane, groundMat);
ground.position.y = -0.02;         // 永远低于 0

// ✅ 正确：共面纹理用统一偏移
const wallBase = new THREE.Mesh(..., wallMat);
wallBase.position.y = 0;
const wallTrim = new THREE.Mesh(..., trimMat);
wallTrim.position.y = wallHeight;   // 错开不重叠

// ✅ 正确：发光叠加层
const glowOverlay = new THREE.Mesh(geo, glowMat);
glowOverlay.position.z = 0.006;    // 微偏移 0.001-0.01
glowOverlay.renderOrder = 1;
glowOverlay.material.depthWrite = false;  // 不写深度、避免遮挡

// ✅ 正确：大跨度场景
renderer.logarithmicDepthBuffer = true;  // 跨 50+ 单位必开

// ✅ 正确：半透明物体排序
transparentObj.renderOrder = 1;
```

### logarithmicDepthBuffer 使用场景

| 场景 | 远近跨度 | 是否需要 |
|------|---------|----------|
| 室内单间 | < 5 单位 | 否 |
| 寺庙+花园 | < 30 单位 | 否 |
| 城市街区 | 20~50 单位 | 建议开 |
| 城市全景（山腰俯瞰） | 30~80 单位 | **必须开** |
| 太空对接 | 5~100+ 单位 | **必须开** |
| 地形远景 | 50~200+ 单位 | **必须开** |

> 警告：logarithmicDepthBuffer 在部分移动端 GPU 上有兼容问题。PC 桌面场景放心开。

---

## 后处理与抗锯齿

### 核心矛盾

**MSAA（硬件抗锯齿）和 EffectComposer（后处理）不能同时生效。**

一旦启用 EffectComposer，必须手动加后处理 AA Pass。

### 方案选择

| 方案 | 质量 | 性能 | 适用 |
|------|------|------|------|
| 不用后处理 + MSAA | ★★★ | ★★★ | 简单场景、无 Bloom |
| EffectComposer + SMAA | ★★☆ | ★★☆ | 主力场景（推荐） |
| EffectComposer + FXAA | ★☆☆ | ★★★ | 移动端/低性能 |
| EffectComposer + 无 AA | ✗ | ★★★ | 仅调试时 |

### 推荐方案代码

```javascript
import { EffectComposer } from 'three/addons/postprocessing/EffectComposer.js';
import { RenderPass } from 'three/addons/postprocessing/RenderPass.js';
import { UnrealBloomPass } from 'three/addons/postprocessing/UnrealBloomPass.js';
import { SMAAPass } from 'three/addons/postprocessing/SMAAPass.js';

// 渲染器 → 关闭硬件 MSAA
const renderer = new THREE.WebGLRenderer({ antialias: false });

// Composer → 渲染 → Bloom → SMAA
const composer = new EffectComposer(renderer);
composer.addPass(new RenderPass(scene, camera));
composer.addPass(new UnrealBloomPass(resolution, 0.12, 0.3, 0.1));
composer.addPass(new SMAAPass(window.innerWidth, window.innerHeight));

// animate 里用 composer.render() 代替 renderer.render()
```

### 各场景 Bloom 强度（复用表）

| 场景类型 | strength | radius | threshold |
|----------|----------|--------|-----------|
| 古风城镇 | 0.18 | 0.3 | 0.1 |
| 太空科幻 | 0.12 | 0.3 | 0.1 |
| 自然地形 | 0.25 | 0.3 | 0.05 |
| 赛博霓虹 | 0.40 | 0.3 | 0.05 |
| 城市黄昏 | 0.12 | 0.3 | 0.1 |
| 月夜幻想 | 0.25 | 0.15 | 0.04 |

---

## GLB 模型导入工作流

### 场景一：Blender MCP 实时协作（推荐新方式）
> 通过 MCP 协议让 AI 直连控制 Blender

**Blender MCP** (ahujasid/blender-mcp, 24.7k stars) 通过 Model Context Protocol 连接 Blender 与 AI 助手。

**安装步骤：**

1. 安装 `uv`：
```bash
# Windows
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

2. 配置 OpenCode MCP（`.opencode.json` 或 opencode 配置）：
```json
{
  "mcp": {
    "blender-mcp": {
      "type": "local",
      "command": ["uvx", "blender-mcp"],
      "enabled": true,
      "environment": {
        "BLENDER_HOST": "localhost",
        "BLENDER_PORT": "9876"
      }
    }
  }
}
```

3. Blender 安装 addon：
   - 从 [ahujasid/blender-mcp](https://github.com/ahujasid/blender-mcp) 下载 `addon.py`
   - Blender → Edit → Preferences → Add-ons → Install → 选择 `addon.py`
   - 勾选启用 "Interface: Blender MCP"
   - 3D View 侧边栏 → BlenderMCP → 点击 "Connect to Claude/LLM"

**工作流示例：**
```
你说："在 Blender 里建一个摩天楼"
→ AI 通过 MCP 调 Blender 创建几何体
→ 你说："导出 GLB 给我"
→ AI 调 Blender 导出 → 得到 GLB 文件
→ 放到洛都市场景里用 GLTFLoader 加载
```

### 场景二：完全用 GLB 替换程序化几何体

```
1. 在 Blender/建模工具中建好模型
2. 导出 GLB（选 "glTF Binary .glb"，勾选 UV + 法线 + 材质）
3. 文件放项目目录，CDN 场景放同目录或可访问 URL
4. 用 GLTFLoader 加载：
```

```javascript
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';

const loader = new GLTFLoader();
loader.load('models/temple.glb', (gltf) => {
  const model = gltf.scene;
  model.position.set(0, 0, 0);
  model.scale.set(1, 1, 1);
  model.traverse((child) => {
    if (child.isMesh) {
      child.castShadow = true;
      child.receiveShadow = true;
    }
  });
  scene.add(model);
});
```

### 场景二：GLB + 程序化混合（推荐）

- **主体建筑**：GLB 精细模型，如神殿/飞船
- **环境**：程序化生成，如树木/岩石/云雾/粒子
- 混合后只需 1-2 个 GLB + 50-200 行程序化环境代码

### 场景三：程序化生成的模型导出为 GLB

用 `GLTFExporter` 把 Three.js 场景导出，供后续复用：

```javascript
import { GLTFExporter } from 'three/addons/exporters/GLTFExporter.js';
const exporter = new GLTFExporter();
exporter.parse(scene, (gltf) => {
  const blob = new Blob([JSON.stringify(gltf)], { type: 'application/json' });
  const url = URL.createObjectURL(blob);
  // 下载或用 url 加载
}, (err) => console.error(err), { binary: true });
```

### GLB 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 模型全黑 | 缺少灯光或法线翻转 | 加 AmbientLight + 检查 Blender 法线 |
| 材质丢失 | Blender 着色器不兼容 | 导出时选 "Bake Modifiers/Apply Modifiers" |
| 纹理路径错误 | GLB 引用外部图片 | 导出时选 "Copy Images" 或手动复制 |
| 缩放不对 | Blender 单位 vs Three.js 单位 | 1 Blender Unit = 1 Three.js Meter |
| 面朝向反了 | Blender 默认面朝向 | 导出时勾选 "+Y Up" (Three.js 默认 +Y)

---

## 踩坑清单（来自 3 个实战场景）

### 长安坊市
| 坑 | 表现 | 修复 |
|----|------|------|
| 台阶 z 起点太靠后 | 第一级台阶嵌入平台 | z 起点 = 平台边缘 + 半步深 |
| 屋顶过于简单 | 像彩色纸盒 | 4 段 Cylinder(0.05→R) + 顶点位移 `1+0.18*pow(1-t,2.5)` 做翘角 |
| 火盆与台阶同 z | 重叠 | 火盆放在平台角，台阶开始前 |
| 只画 BoxGeometry 没有柱子 | 不像中式建筑 | 每栋楼至少加 4 根角柱 + 檐板 |
| Bloom 超过 0.3 | 古风发糊，像过曝 | 古风 0.15-0.20，赛博才用 0.3+ |
| shadow bias 过小 | 条纹/斑块阴影 | 调 bias 到 -0.002 ~ -0.004 |

### 空间站对接
| 坑 | 表现 | 修复 |
|----|------|------|
| 环段旋转角算错 | 居住舱朝向错误 | Y 轴旋转公式 `-(angle + PI/2)` |
| 辐条用 rotation.z | 辐条倾斜落不在径线 | 改用 `rotation.y = PI/2 - angle` |
| 背阳面过暗 | 暗部全黑看不清结构 | 加 fill light + ambient + 地球反照光 |
| 地球 Shader 没传 sunDir | 昼夜不分全蓝 | uniform 在 animate 里每帧更新 |
| 材质太多各自 new | 内存飙升、代码臃肿 | 统一 M.* 对象，全局共享 |
| 太阳能板缺栅格线 | 远看像纯蓝方块 | 每片面板叠加 Box 栅格线 + 边框 |

### 圣林神殿
| 坑 | 表现 | 修复 |
|----|------|------|
| 台阶在平台内部 | 视觉重叠 | 台阶 z 起点对齐平台外缘 |
| 山墙用 ExtrudeGeometry 复制 | 屋顶双重面 z-fighting | 换成 ShapeGeometry 薄三角面贴面 |
| 火盆与台阶 z 同线 | 模型穿插 | 火盆移到平台两角（x±1.4, z1.05） |
| 巨树根直径不够大 | 看起来不像古树 | 树根直径 > 树冠半径 × 2 |
| 鸟群飞出画面不回来 | V 字消失 | position 超界时取反镜像折回 |
| fog color 与场景不匹配 | 雾色突兀 | fog 色 = 天空色，雾密度 ≤ 0.005 |

### 洛都市
| 坑 | 表现 | 修复 |
|----|------|------|
| 山腰视角远建筑闪烁 | 远处楼群抖动 | 开启 `logarithmicDepthBuffer: true` |
| 地面与高速路同级 | 道路埋入地面 | 地面 y = -0.02，路面 y = 0.05 |
| 棕榈树 frond 穿模 | 树叶穿透另一片树叶 | frond 旋转角错开 + 随机扰动 |
| 小车与路面高度不一致 | 车半截陷进路/飘起 | 车身 y = wheelGap + bodyHalfHeight（≈ 0.03+0.04） |
| 高楼窗光太高频 | 远处闪烁刺眼 | winGlow opacity 限 0.05-0.15，depthWrite: false |
| 城市辉光面覆盖建筑 | 建筑被发光半透明平面遮挡 | renderOrder = 1，depthWrite = false |
| 高速公路 z 坐标正弦过强 | 道路歪出视野 | sin 振幅 ≤ 2 单位 |
| 车灯叠加层遮挡阴影 | 阴影被灯球吃掉 | 车灯 `castShadow = false`，`receiveShadow = false` |
| 建筑随机撒点重叠 | 楼与楼互相穿模 | 改用网格坐标 + `overlaps()` 碰撞检测 |
| `const pillarGeo` 重复声明 | 运行时 `SyntaxError` | 同名变量不可在同一模块 `const` 两次，楼角柱 vs 天桥柱分别命名 |
| `matLookup` 缺失 | `ReferenceError: matLookup is not defined` | 统一材质键表放在建筑段首 |
| LOADING 依托水纹理回调隐藏 | 水纹理 CDN 404/卡顿时 LOADING 永驻 | 同步场景构建完即隐藏 + `setTimeout(…, 5000)` 兜底 |
| 水纹理 URL 404 | `waternormals.jpg` CDN 路径错误 | 用 `threejs.org/examples/textures/` 替代 `jsdelivr` |
| 自定义海滩顶点排列错误 | 沙滩竖起来（`rotation.x = -π/2` 后 y 随 z 变化） | 顶点用 `(x, z, 0)` 而非 `(x, 0, z)` — 匹配 PlaneGeometry 布局 |
| 海洋覆盖城区 | 水体 x 范围 [-5,35] 与 Downtown 重叠 | 海洋右移至 x=18，沙滩置于城市与海洋之间 |
| `new THREE.FogExp2()` 每帧创建 | 内存泄漏 + 垃圾回收压力 | 在 animate 外声明复用，每帧只调 `scene.fog = fog` |
| `new THREE.Vector3()` 每帧创建 | 同上一级 GC 压力 | 声明全局变量 `_v = new V3()`，每帧 `.lerpVectors` 复用 |

---

## 入门路线图

### 零基础 → 能跟着 AI 出场景

```
第 1 步：看懂 Three.js 基础概念（30 分钟）
  - Scene / Camera / Renderer 三角关系
  - 几何体（Box, Cylinder, Sphere, Torus, Cone, Plane）
  - 材质（Standard vs Basic vs Shader）
  - 光源（Directional vs Ambient vs Hemisphere）

第 2 步：克隆一个场景跑起来（15 分钟）
  - 复制 长安坊市.html 或 空间站对接.html
  - 用 Live Server / 浏览器直接打开
  - 拖拽看看、改改颜色试试

第 3 步：跟 AI 走一轮完整工作流（30 分钟）
  - 给 AI 一句话描述
  - 逐轮选维度
  - 拿到生成的 HTML 打开看效果
  - 对着代码找 create* 函数，尝试改一个参数

第 4 步：自己做一个小改动（1 小时）
  - 比如"把神殿火盆直径从 0.13 改成 0.2"
  - 或者"把空间站环形半径从 3.8 改成 6"
  - 改完 Ctrl+S → 刷新浏览器 → 看变化

第 5 步：学 GLB 导入（1 小时）
  - Blender 建一个简单的柱子
  - 导出 GLB
  - 用上面的 GLTFLoader 代码加载替换现有柱子
```

### 进阶路线

```
- ShaderMaterial 入门：改 Earth 的 fragmentShader 里的颜色试试
- 学会用 BufferGeometry 自定义形状
- 了解 InstancedMesh（性能优化必备）
- 了解 React Three Fiber（R3F）
```

### 推荐学习资源

| 资源 | 用途 |
|------|------|
| [threejs.org/docs](https://threejs.org/docs) | 官方文档，每个 API 都有示例 |
| [threejs.org/editor](https://threejs.org/editor) | 浏览器内可视化编辑器，拖拽搭建场景 |
| [Bruno Simon - Three.js Journey](https://threejs-journey.com) | 最系统的付费课程 |
| [discoverthreejs.com](https://discoverthreejs.com) | 免费入门书 |
| [dragonir/3d](https://github.com/dragonir/3d) | 纯 Three.js 多场景中文实例集 |
